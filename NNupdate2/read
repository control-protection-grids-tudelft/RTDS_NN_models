Use trainNN.py to train a model in Python. The weights of the models can then be updated by updating their respective C file. The weight definintions are found in the STATIC section for the layered components 
and in the CODE_FUNCTIONS section in the Full Models.

txt script RSCAD_DataCollect is an example script for how to extract the data files iteratively from RSCAD as certain variables are changed. In the Example only the reference Active power is changed, 
you can include changes in other variables by following the same script logic
